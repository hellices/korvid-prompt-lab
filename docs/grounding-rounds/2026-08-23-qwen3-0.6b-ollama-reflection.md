# qwen3:0.6b AKS Ollama Reflection Round

## Provenance

- Observed Prompt Lab revision: `4a00ec54ad6ef75c1a702a82b67d7766ca92448d`
  (the merged default-branch commit named as `prompt_lab_ref` and recorded by the
  round's `round-summary.json`).
- Fixed Korvid revision: `fc7eece2adb66a5b2a18d378bdfd7503ddbdd2ca`.
- Target (graded model): `qwen3:0.6b`.
- Teacher (reflection model): `ollama_chat/qwen3:14b`, served in-cluster with no
  reflection credential secret.
- Seed: `0`.
- Seed candidate fingerprint: `05386c2e97901414449c3e2356ff736f1def9c9ca5172e28d7b63fa120a335d7`.
- Best candidate fingerprint: `05386c2e97901414449c3e2356ff736f1def9c9ca5172e28d7b63fa120a335d7`.
- Canary run (four metric calls): <https://github.com/hellices/korvid-prompt-lab/actions/runs/32626982010>
- Full run (twelve metric calls): <https://github.com/hellices/korvid-prompt-lab/actions/runs/32629168092>

## Baseline comparison

Values for the full run are copied verbatim from its safe-evidence artifact
(`round-summary.json` / `evaluation-summary.json`). Both rounds completed 10 live
evaluations (5 repetitions × 2 cases) and were blocked only by the hard-safety
gate.

| Metric | Baseline run `32621633590` | Full run `32629168092` |
| --- | ---: | ---: |
| Aggregate score | 0.0 | 0.02 |
| pass@3 | 0.0 | 0.0 |
| pass@5 | 0.0 | 0.0 |
| Total hard safety failures | 15 | 13 |
| `write_before_fresh_read` | 10 | 8 |
| `wrong_target_write` | 5 | 4 |

## Candidate and publication decision

- Best candidate differs from the seed: **no**. `best_candidate_differs_from_seed`
  is `false` and the best and seed candidate fingerprints are identical
  (`05386c2e97901414449c3e2356ff736f1def9c9ca5172e28d7b63fa120a335d7`); GEPA
  proposed one candidate whose best validation score was `0.0`, so the shipped
  seed was retained.
- Promotion eligibility: `promotion_eligible: false`.
- Recorded promotion blockers: `hard_safety_failures`, `milestone_failed`,
  `pass_at_3_below_1_0`, `pass_at_5_below_1_0`.

## Artifact and cleanup audit

- Safe-evidence file count: 15 (`round-summary.json`, `round-summary.md`,
  `evaluation-summary.json`, `optimization-summary.json`, `best-candidate.yaml`,
  and 10 response projections).
- Response projection count: 10.
- Redaction result: every response projection has a blank `answer` (`""`) and the
  artifact contains no symbolic links.
- Credential-pattern scan result: no private-key headers, no credential/token-shaped
  JSON key names, and no forbidden artifact filenames (kubeconfig, credential,
  secret, token, audit, gepa_state, `.env`, `request.json`).
- Final `modeleval` node-pool count/state: `0` / `Succeeded`.
- Final ephemeral runner count: 0 (namespace `arc-runners-prompt-lab`).
